<div class="space-y-6">
  <div class="flex justify-between items-center">
    <h1 class="text-2xl font-bold text-gray-900">Accounts</h1>
    <%= link_to "New Account", new_account_path, class: "bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700" %>
  </div>

  <div class="bg-white rounded-lg shadow overflow-hidden">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Name</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Type</th>
          <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">Balance</th>
          <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Currency</th>
          <th class="px-6 py-3 actions"></th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-200">
        <% @accounts.each do |account| %>
          <tr>
            <td class="px-6 py-4 whitespace-nowrap font-medium"><%= account.name %></td>
            <td class="px-6 py-4 whitespace-nowrap text-sm"><%= account.account_type || "-" %></td>
            <td class="px-6 py-4 whitespace-nowrap text-right font-medium">¥<%= account.current_balance.to_s("%.2f") %></td>
            <td class="px-6 py-4 whitespace-nowrap text-sm"><%= account.currency %></td>
            <td class="px-6 py-4 whitespace-nowrap text-right text-sm">
              <%= link_to "Edit", edit_account_path(account), class: "text-blue-600 hover:text-blue-900" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <% if @accounts.empty? %>
      <div class="p-8 text-center text-gray-500">
        No accounts yet
      </div>
    <% end %>
  </div>
</div>

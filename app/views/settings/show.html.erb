<div class="space-y-6">
  <h1 class="text-2xl font-semibold text-primary">设置</h1>

  <!-- Import Results -->
  <% if flash[:warning] %>
    <div class="bg-yellow-50 border border-yellow-200 text-yellow-800 rounded-lg p-4">
      <p class="font-medium"><%= flash[:warning] %></p>
      <% if flash[:import_errors].present? %>
        <ul class="mt-2 text-sm list-disc list-inside">
          <% flash[:import_errors].first(10).each do |error| %>
            <li><%= error %></li>
          <% end %>
          <% if flash[:import_errors].size > 10 %>
            <li>... 还有 <%= flash[:import_errors].size - 10 %> 个错误</li>
          <% end %>
        </ul>
      <% end %>
    </div>
  <% end %>

  <!-- Currency Settings -->
  <div class="bg-container rounded-lg shadow-border-xs">
    <div class="p-4 border-b border-border">
      <h2 class="text-base font-semibold text-primary">货币设置</h2>
      <p class="text-sm text-secondary mt-1">管理货币和汇率</p>
    </div>
    <div class="p-4">
      <table class="min-w-full">
        <thead>
          <tr>
            <th class="text-left text-xs font-medium text-secondary uppercase tracking-wider">代码</th>
            <th class="text-left text-xs font-medium text-secondary uppercase tracking-wider">名称</th>
            <th class="text-left text-xs font-medium text-secondary uppercase tracking-wider">符号</th>
            <th class="text-left text-xs font-medium text-secondary uppercase tracking-wider">汇率</th>
            <th class="text-right"></th>
          </tr>
        </thead>
        <tbody>
          <% @currencies.each do |currency| %>
            <tr class="border-t border-border">
              <td class="py-3 font-medium text-primary"><%= currency.code %></td>
              <td class="py-3 text-secondary"><%= currency.name %></td>
              <td class="py-3"><%= currency.symbol %></td>
              <td class="py-3 text-secondary"><%= currency.exchange_rate %></td>
              <td class="py-3 text-right">
                <button class="text-sm text-blue-600 hover:text-blue-700">编辑</button>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <% if @currencies.empty? %>
        <p class="text-center text-secondary py-4">暂无货币配置</p>
      <% end %>
      <button class="mt-4 text-sm text-blue-600 hover:text-blue-700 font-medium">+ 添加货币</button>
    </div>
  </div>

  <!-- Data Management -->
  <div class="bg-container rounded-lg shadow-border-xs">
    <div class="p-4 border-b border-border">
      <h2 class="text-base font-semibold text-primary">数据管理</h2>
      <p class="text-sm text-secondary mt-1">导入、导出和备份您的数据</p>
    </div>
    <div class="p-4 space-y-4">
      <!-- Export -->
      <div class="flex items-center justify-between py-3 border-b border-border">
        <div>
          <h3 class="font-medium text-primary">导出数据</h3>
          <p class="text-sm text-secondary">下载所有交易记录为 CSV 文件</p>
        </div>
        <%= button_to "导出 CSV", export_transactions_path, method: :post, class: "px-4 py-2 text-sm font-medium rounded-lg bg-gray-200 text-primary hover:bg-gray-300 transition-smooth" %>
      </div>

      <!-- Import -->
      <div class="py-3 border-b border-border">
        <div class="flex items-start justify-between">
          <div class="flex-1">
            <h3 class="font-medium text-primary">导入数据</h3>
            <p class="text-sm text-secondary">从 CSV 文件导入交易记录</p>
          </div>
        </div>
        <%= form_with url: import_transactions_path, method: :post, local: true, class: "mt-3", multipart: true do |f| %>
          <div class="flex items-center gap-3">
            <%= f.file_field :file, accept: ".csv", class: "text-sm text-primary border border-border rounded-lg p-2", required: true %>
            <%= f.submit "导入", class: "px-4 py-2 text-sm font-medium rounded-lg bg-gray-200 text-primary hover:bg-gray-300 transition-smooth" %>
          </div>
          <p class="text-xs text-secondary mt-2">
            CSV 格式：日期, 类型(收入/支出), 金额, 账户, 分类, 目标账户, 备注
          </p>
        <% end %>
      </div>

      <!-- Backup -->
      <div class="py-3">
        <div class="flex items-center justify-between mb-3">
          <div>
            <h3 class="font-medium text-primary">数据库备份</h3>
            <p class="text-sm text-secondary">创建完整的数据库备份</p>
          </div>
          <%= button_to "创建备份", create_backup_path, method: :post, class: "px-4 py-2 text-sm font-medium rounded-lg bg-gray-200 text-primary hover:bg-gray-300 transition-smooth" %>
        </div>
        <% if @backups.any? %>
          <div class="bg-surface-inset rounded-lg p-3">
            <p class="text-xs font-medium text-secondary mb-2">最近的备份：</p>
            <ul class="space-y-1">
              <% @backups.each do |backup| %>
                <li class="flex items-center justify-between text-sm">
                  <span class="text-secondary">
                    <%= backup[:name] %> (<%= number_to_human_size(backup[:size]) %>)
                  </span>
                  <%= link_to "下载", download_backup_path(backup[:name]), class: "text-blue-600 hover:text-blue-700" %>
                </li>
              <% end %>
            </ul>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Display Settings -->
  <div class="bg-container rounded-lg shadow-border-xs">
    <div class="p-4 border-b border-border">
      <h2 class="text-base font-semibold text-primary">显示设置</h2>
      <p class="text-sm text-secondary mt-1">自定义应用显示偏好</p>
    </div>
    <div class="p-4 space-y-4">
      <div class="flex items-center justify-between py-3 border-b border-border">
        <div>
          <h3 class="font-medium text-primary">默认货币</h3>
          <p class="text-sm text-secondary">新账户的默认货币</p>
        </div>
        <select class="px-3 py-2 text-sm rounded-lg border border-border focus:ring-blue-500 focus:border-blue-500">
          <option>CNY (¥)</option>
          <option>USD ($)</option>
          <option>EUR (€)</option>
        </select>
      </div>
      <div class="flex items-center justify-between py-3 border-b border-border">
        <div>
          <h3 class="font-medium text-primary">日期格式</h3>
          <p class="text-sm text-secondary">选择日期显示格式</p>
        </div>
        <select class="px-3 py-2 text-sm rounded-lg border border-border focus:ring-blue-500 focus:border-blue-500">
          <option>2024-01-15</option>
          <option>2024/01/15</option>
          <option>01-15-2024</option>
        </select>
      </div>
      <div class="flex items-center justify-between py-3">
        <div>
          <h3 class="font-medium text-primary">起始日期</h3>
          <p class="text-sm text-secondary">仪表盘默认显示的月份</p>
        </div>
        <select class="px-3 py-2 text-sm rounded-lg border border-border focus:ring-blue-500 focus:border-blue-500">
          <option>当前月份</option>
          <option>上个月</option>
          <option>年初</option>
        </select>
      </div>
    </div>
  </div>

  <!-- Danger Zone -->
  <div class="bg-container rounded-lg shadow-border-xs border border-red-200">
    <div class="p-4 border-b border-red-200">
      <h2 class="text-base font-semibold text-red-600">危险区域</h2>
      <p class="text-sm text-secondary mt-1">以下操作不可逆，请谨慎操作</p>
    </div>
    <div class="p-4 space-y-4">
      <div class="flex items-center justify-between py-3 border-b border-red-100">
        <div>
          <h3 class="font-medium text-primary">清除所有数据</h3>
          <p class="text-sm text-secondary">删除所有交易、账户和分类数据</p>
        </div>
        <%= button_to "清除数据", clear_all_data_path, method: :post, 
            data: { confirm: "确定要清除所有数据吗？此操作不可恢复！", turbo_confirm: "确定要清除所有数据吗？此操作不可恢复！" },
            class: "px-4 py-2 text-sm font-medium rounded-lg bg-red-100 text-red-600 hover:bg-red-200 transition-smooth" %>
      </div>
      <div class="flex items-center justify-between py-3">
        <div>
          <h3 class="font-medium text-primary">重置应用</h3>
          <p class="text-sm text-secondary">恢复所有设置为默认值</p>
        </div>
        <button class="px-4 py-2 text-sm font-medium rounded-lg bg-red-100 text-red-600 hover:bg-red-200 transition-smooth">重置</button>
      </div>
    </div>
  </div>
</div>

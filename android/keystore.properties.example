# Keystore 签名配置
# ⚠️ 此文件不提交到 Git，包含敏感信息
# 由 scripts/generate_keystore.sh 自动生成
# 或手动创建，格式如下：

storePassword=your_store_password
keyPassword=your_key_password
keyAlias=ledger
storeFile=keystore/ledger-release.jks

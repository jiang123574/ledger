# 分类数据迁移记录

## 执行时间
2026-04-08

## 迁移目的
清理冗余分类，将特定分类的交易归类到"其他"分类。

---

## 迁移内容

### 1. 支出分类迁移

**删除分类**: 22个包含"垫付"或"借给"字样的支出分类

迁移的交易:
- transactions表: 342笔
- entryable_transactions表: 338笔

**目标分类**: "其他" (ID: 2204)

### 2. 收入分类迁移

**删除分类**: 56个包含"收款"字样的收入分类

迁移的交易:
- transactions表: 102笔
- entryable_transactions表: 77笔

**目标分类**: "其他收入" (ID: 2257)

---

## 迁移后状态

### 分类数量
- 支出分类: 108个
- 收入分类: 28个
- 删除总计: 78个分类

### 目标分类统计
- "其他"支出: 616笔交易
- "其他收入": 291笔交易

---

## 技术细节

### 外键约束处理
迁移时需要处理两个表的外键约束:
1. `transactions.category_id`
2. `entryable_transactions.category_id`

### 执行流程
1. 更新transactions表的category_id
2. 更新entryable_transactions表的category_id
3. 删除空分类
4. 清除缓存

### 事务安全
整个迁移在一个数据库事务中完成，确保数据一致性。

---

## 影响范围

### 用户影响
- 原"垫付/借给"分类的交易现显示为"其他"支出
- 原"收款"分类的交易现显示为"其他收入"
- 不影响交易金额、日期、账户等其他信息

### 统计影响
- 支出/收入统计不受影响
- 分类报表中的分类数量减少
- "其他"分类的交易数量增加

---

## 数据完整性验证

✅ 所有目标分类已删除
✅ 无外键约束错误
✅ 交易总数不变
✅ 应用运行正常

---

## 回滚方案

如需回滚，需要:
1. 重新创建被删除的分类
2. 从备份中恢复category_id关联
3. 清除缓存

**注意**: 由于分类已被删除，无法自动回滚。

---

## 相关表

- `categories` - 分类表
- `transactions` - 交易表（旧）
- `entryable_transactions` - 交易表（新）
- `entries` - 分录表

---

## 缓存清理

迁移后执行了:
```ruby
CacheBuster.bump(:entries)
CacheBuster.bump(:accounts)
```

---

## 注意事项

1. **两个交易表**: 本项目有两个交易表
   - `transactions` - 旧表，仍有外键约束
   - `entryable_transactions` - 新表，主要使用

2. **分类数量不一致**: 两个表的记录数不同，这是正常的（新旧数据差异）

3. **后续维护**: 建议统一使用 `entryable_transactions` 表，逐步废弃 `transactions` 表

---

**迁移完成** ✅